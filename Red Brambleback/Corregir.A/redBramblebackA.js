// Función anónima para elevar al cubo
const cubo = function (x) {
  x * x * x;
};

// Intenta utilizar la función almacenada para calcular el cubo de 3
const resultado = cubo(3);
console.log(resultedo);
